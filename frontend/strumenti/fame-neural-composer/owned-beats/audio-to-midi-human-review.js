"use strict";

const crypto=require("node:crypto");
const fs=require("node:fs");
const http=require("node:http");
const path=require("node:path");
const {spawn}=require("node:child_process");
const qa=require("./audio-to-midi-development-qa");

const HERE=__dirname;
const REVIEW_PROTOCOL_FILE=path.join(HERE,"audio-to-midi-human-review-v1.json");
const DEFAULT_REVIEW_ID="audio-to-midi-human-review-v1-001";
const RUN_ID="audio-to-midi-development-baseline-v1-001";
const SOURCE_SEP_RUN_ID="source-separation-development-inference-v1-001";
const PUBLIC_PACKAGE_SCHEMA="fame-owned-beats-audio-to-midi-human-review-package-v1";
const SUBMISSION_SCHEMA="fame-owned-beats-audio-to-midi-human-review-submission-v1";
const REPORT_SCHEMA="fame-owned-beats-audio-to-midi-human-review-report-v1";

function readJson(file){return JSON.parse(fs.readFileSync(file,"utf8").replace(/^\uFEFF/,""))}
function stableJson(v){return JSON.stringify(v,null,2)+"\n"}
function sha256File(file){return crypto.createHash("sha256").update(fs.readFileSync(file)).digest("hex")}
function median(values){const s=values.slice().sort((a,b)=>a-b),m=s.length/2;return s.length%2?s[Math.floor(m)]:(s[m-1]+s[m])/2}
function safeId(v,label){if(typeof v!=="string"||!/^[A-Za-z0-9][A-Za-z0-9._-]{2,79}$/.test(v))throw new Error(label+" invalid");return v}
function reviewRoot(workspace,reviewId){return path.join(workspace,"reviews","audio-to-midi-development",safeId(reviewId,"review-id"))}
function runRoot(workspace){return path.join(workspace,"runs","audio-to-midi-development-baseline",RUN_ID)}
function sourceSepRoot(workspace){return path.join(workspace,"runs","source-separation-development-inference",SOURCE_SEP_RUN_ID)}

function protocol(){
 const p=readJson(REVIEW_PROTOCOL_FILE);
 if(p.schema!=="fame-owned-beats-audio-to-midi-human-review-v1"||p.version!==1||p.status!=="FROZEN_BEFORE_FIRST_AUDIO_TO_MIDI_LISTENING"||
 p.reviewId!==DEFAULT_REVIEW_ID||p.runId!==RUN_ID||p.split!=="development"||p.expectedFamilies!==8||p.blindness?.drums!==true||
 p.blindness?.armIdentityExposedDuringReview!==false||p.renderer?.sameRendererForBothDrumArms!==true||
 p.drumsSelectionRule?.qualification?.medianUsefulnessAtLeast!==2||p.drumsSelectionRule?.qualification?.familiesAtOrAbove2AtLeast!==6||
 p.lowEndGate?.armId!=="librosa-pyin-lowend-v1"||p.lowEndGate?.medianUsefulnessAtLeast!==2||p.lowEndGate?.familiesAtOrAbove2AtLeast!==6||
 p.safety?.finalHoldoutAllowed!==false||p.safety?.batch131Allowed!==false||p.safety?.trainingAuthorized!==false)throw new Error("Frozen Audio→MIDI Human Review protocol invalid");
 return p;
}

function balancedBlindMap(ids){
 const ranked=ids.map(id=>({id,hash:crypto.createHash("sha256").update(DEFAULT_REVIEW_ID+"|"+id).digest("hex")})).sort((a,b)=>a.hash.localeCompare(b.hash));
 const aIsDrumsOnly=new Set(ranked.slice(0,Math.floor(ids.length/2)).map(x=>x.id));
 const out={};for(const id of ids)out[id]=aIsDrumsOnly.has(id)?{A:"drumsOnly",B:"drumsBassKickFusion"}:{A:"drumsBassKickFusion",B:"drumsOnly"};return out;
}

function prepare(workspaceRoot,reviewId=DEFAULT_REVIEW_ID){
 reviewId=safeId(reviewId,"review-id");if(reviewId!==DEFAULT_REVIEW_ID)throw new Error("Only frozen reviewId is allowed");
 const workspace=path.resolve(workspaceRoot);const technical=qa.technical(workspace,RUN_ID);
 if(technical.mode!=="AUDIO_TO_MIDI_DEVELOPMENT_TECHNICAL_QA_PASS"||technical.technicalGate!=="ALL_8_FAMILIES_PASS"||technical.recordsVerified!==8||technical.midiFilesVerified!==24)throw new Error("Technical QA must pass before Human Review");
 const p=protocol(),root=reviewRoot(workspace,reviewId);if(fs.existsSync(root))throw new Error("Append-only review already exists: "+root);
 const summary=readJson(path.join(runRoot(workspace),"summary.json")),ids=summary.results.map(x=>x.sourceRecordId);if(ids.length!==8)throw new Error("Unexpected family coverage");
 const blind=balancedBlindMap(ids),families=ids.map(id=>({sourceRecordId:id,drumCandidates:["A","B"],lowEndCandidate:"pYIN"}));
 const pkg={schema:PUBLIC_PACKAGE_SCHEMA,version:1,status:"PREPARED_AWAITING_HUMAN_REVIEW",reviewId,runId:RUN_ID,preparedAt:new Date().toISOString(),protocolDigestSha256:sha256File(REVIEW_PROTOCOL_FILE),technicalGate:"ALL_8_FAMILIES_PASS",renderer:p.renderer,scale:p.scale,drumsCriteria:p.drumsCriteria,lowEndCriteria:p.lowEndCriteria,families,safety:p.safety};
 fs.mkdirSync(path.dirname(root),{recursive:true});fs.mkdirSync(root,{recursive:false});
 try{fs.writeFileSync(path.join(root,"review-package.json"),stableJson(pkg),{flag:"wx"});fs.writeFileSync(path.join(root,"blind-key.json"),stableJson({schema:"fame-owned-beats-audio-to-midi-human-review-blind-key-v1",version:1,reviewId,mapping:blind}),{flag:"wx"})}
 catch(error){fs.rmSync(root,{recursive:true,force:true});throw error}
 return{mode:"AUDIO_TO_MIDI_HUMAN_REVIEW_PREPARED",reviewId,records:8,drumsArmIdentityExposedToReviewer:false,finalHoldoutAccessedByThisCommand:false,trainingAuthorized:false,nextAction:"SERVE_AND_COMPLETE_BLIND_HUMAN_REVIEW"};
}

function wavHeader(dataBytes,sampleRate=22050){const b=Buffer.alloc(44);b.write("RIFF",0,"ascii");b.writeUInt32LE(36+dataBytes,4);b.write("WAVE",8,"ascii");b.write("fmt ",12,"ascii");b.writeUInt32LE(16,16);b.writeUInt16LE(1,20);b.writeUInt16LE(1,22);b.writeUInt32LE(sampleRate,24);b.writeUInt32LE(sampleRate*2,28);b.writeUInt16LE(2,32);b.writeUInt16LE(16,34);b.write("data",36,"ascii");b.writeUInt32LE(dataBytes,40);return b}
function toWav(samples,sr=22050){let peak=0;for(const v of samples)peak=Math.max(peak,Math.abs(v));const scale=peak>0.98?0.98/peak:1,pcm=Buffer.alloc(samples.length*2);for(let i=0;i<samples.length;i++){const x=Math.max(-1,Math.min(1,samples[i]*scale));pcm.writeInt16LE(Math.round(x*32767),i*2)}return Buffer.concat([wavHeader(pcm.length,sr),pcm])}
function seededNoise(seed){let state=(seed>>>0)||1;return()=>{state=(1664525*state+1013904223)>>>0;return(state/4294967296)*2-1}}

function addDrum(samples,sr,event,index){
 const start=Math.max(0,Math.floor(event.timeSeconds*sr));
 if(event.role==="kick"){const dur=Math.floor(.22*sr);let phase=0;for(let i=0;i<dur&&start+i<samples.length;i++){const t=i/sr,env=Math.exp(-t*18),f=45+85*Math.exp(-t*25);phase+=2*Math.PI*f/sr;samples[start+i]+=.75*env*Math.sin(phase)}}
 else if(event.role==="snare"){const dur=Math.floor(.16*sr),rnd=seededNoise(index*7919+17);let phase=0,hpPrev=0,xPrev=0;for(let i=0;i<dur&&start+i<samples.length;i++){const t=i/sr,env=Math.exp(-t*22),x=rnd(),hp=.93*(hpPrev+x-xPrev);hpPrev=hp;xPrev=x;phase+=2*Math.PI*180/sr;samples[start+i]+=.32*env*hp+.18*env*Math.sin(phase)}}
 else{const dur=Math.floor(.07*sr),rnd=seededNoise(index*104729+23);let hpPrev=0,xPrev=0;for(let i=0;i<dur&&start+i<samples.length;i++){const t=i/sr,env=Math.exp(-t*55),x=rnd(),hp=.97*(hpPrev+x-xPrev);hpPrev=hp;xPrev=x;samples[start+i]+=.22*env*hp}}
}
function renderDrums(events,sr=22050){const end=Math.max(1,...events.map(e=>e.timeSeconds+.35)),samples=new Float32Array(Math.ceil(end*sr));events.forEach((e,i)=>addDrum(samples,sr,e,i));return toWav(samples,sr)}
function midiHz(note){return 440*Math.pow(2,(note-69)/12)}
function renderBassNotes(notes,sr=22050){const end=Math.max(1,...notes.map(n=>n.endSeconds+.1)),samples=new Float32Array(Math.ceil(end*sr));notes.forEach(n=>{const start=Math.max(0,Math.floor(n.startSeconds*sr)),stop=Math.min(samples.length,Math.ceil(n.endSeconds*sr)),f=midiHz(n.midiNote);let phase=0;for(let i=start;i<stop;i++){const t=(i-start)/sr,remain=(stop-i)/sr,env=Math.min(1,t/.01,remain/.04);phase+=2*Math.PI*f/sr;samples[i]+=.36*env*(Math.sin(phase)+.22*Math.sin(2*phase))}});return toWav(samples,sr)}
function renderContour(points,sr=22050){const end=Math.max(1,...points.map(p=>p.timeSeconds+.05)),samples=new Float32Array(Math.ceil(end*sr));if(points.length<2)return toWav(samples,sr);let phase=0;for(let j=0;j<points.length-1;j++){const a=points[j],b=points[j+1],gap=b.timeSeconds-a.timeSeconds;if(gap<=0||gap>.05)continue;const start=Math.max(0,Math.floor(a.timeSeconds*sr)),stop=Math.min(samples.length,Math.ceil(b.timeSeconds*sr));for(let i=start;i<stop;i++){const u=(i/sr-a.timeSeconds)/gap,f=a.frequencyHz+(b.frequencyHz-a.frequencyHz)*Math.max(0,Math.min(1,u));phase+=2*Math.PI*f/sr;samples[i]+=.32*Math.sin(phase)}}return toWav(samples,sr)}

function sourceStemPath(workspace,rid,stem){const receipt=readJson(path.join(sourceSepRoot(workspace),"execution-receipt.json")),source=receipt.sources.find(x=>x.sourceRecordId===rid);if(!source)throw new Error("Unknown source record");return path.join(sourceSepRoot(workspace),source.outputRelativePath,stem+".wav")}
function resultFor(workspace,rid){return readJson(path.join(runRoot(workspace),rid,"result.json"))}
function computeArmStats(values,qual){const scores=values.map(x=>x.score),med=median(scores),atLeast=scores.filter(x=>x>=2).length;return{medianUsefulness:med,familiesAtOrAbove2:atLeast,totalUsefulness:scores.reduce((a,b)=>a+b,0),pass:med>=qual.medianUsefulnessAtLeast&&atLeast>=qual.familiesAtOrAbove2AtLeast}}
function chooseDrums(stats){const c=Object.entries(stats).filter(([,v])=>v.pass);if(!c.length)return null;c.sort((a,b)=>{if(b[1].medianUsefulness!==a[1].medianUsefulness)return b[1].medianUsefulness-a[1].medianUsefulness;if(b[1].familiesAtOrAbove2!==a[1].familiesAtOrAbove2)return b[1].familiesAtOrAbove2-a[1].familiesAtOrAbove2;if(b[1].totalUsefulness!==a[1].totalUsefulness)return b[1].totalUsefulness-a[1].totalUsefulness;if(a[0]==="drums-only-spectral-onset-v1")return-1;if(b[0]==="drums-only-spectral-onset-v1")return 1;return a[0].localeCompare(b[0])});return c[0][0]}

function validateSubmission(doc,pkg){
 if(doc?.schema!==SUBMISSION_SCHEMA||doc.version!==1||doc.reviewId!==pkg.reviewId||doc.packageDigestSha256!==pkg.packageDigestSha256||!Array.isArray(doc.families)||doc.families.length!==8)throw new Error("Invalid Human Review submission envelope");
 const ids=new Set(pkg.families.map(x=>x.sourceRecordId)),seen=new Set();for(const item of doc.families){if(!ids.has(item.sourceRecordId)||seen.has(item.sourceRecordId))throw new Error("Invalid/duplicate family");seen.add(item.sourceRecordId);for(const label of["A","B"]){const score=item.drums?.[label]?.score;if(!Number.isInteger(score)||score<0||score>3)throw new Error("Invalid drums score")}const low=item.lowEnd?.score;if(!Number.isInteger(low)||low<0||low>3)throw new Error("Invalid low-end score");if(item.reviewerAttested!==true)throw new Error("Reviewer attestation missing")}return doc;
}
function finalizeSubmission(doc,pkg,key,p){
 validateSubmission(doc,pkg);const byArm={"drums-only-spectral-onset-v1":[],"drums-bass-kick-fusion-v1":[]},low=[],families=[];
 for(const item of doc.families){const mapping=key.mapping[item.sourceRecordId];for(const label of["A","B"]){const armKey=mapping[label],armId=armKey==="drumsOnly"?"drums-only-spectral-onset-v1":"drums-bass-kick-fusion-v1";byArm[armId].push({sourceRecordId:item.sourceRecordId,score:item.drums[label].score,note:item.drums[label].note||""})}low.push({sourceRecordId:item.sourceRecordId,score:item.lowEnd.score,note:item.lowEnd.note||""});families.push({sourceRecordId:item.sourceRecordId,drums:{A:{armId:mapping.A==="drumsOnly"?"drums-only-spectral-onset-v1":"drums-bass-kick-fusion-v1",...item.drums.A},B:{armId:mapping.B==="drumsOnly"?"drums-only-spectral-onset-v1":"drums-bass-kick-fusion-v1",...item.drums.B}},lowEnd:item.lowEnd,reviewerAttested:true})}
 const q=p.drumsSelectionRule.qualification,stats={"drums-only-spectral-onset-v1":computeArmStats(byArm["drums-only-spectral-onset-v1"],q),"drums-bass-kick-fusion-v1":computeArmStats(byArm["drums-bass-kick-fusion-v1"],q)},selected=chooseDrums(stats),lowStats=computeArmStats(low,{medianUsefulnessAtLeast:p.lowEndGate.medianUsefulnessAtLeast,familiesAtOrAbove2AtLeast:p.lowEndGate.familiesAtOrAbove2AtLeast});
 return{families,gate:{technical:"ALL_8_FAMILIES_PASS",drums:{arms:stats,selectedArm:selected,pass:selected!==null},lowEnd:{armId:"librosa-pyin-lowend-v1",...lowStats},pass:selected!==null&&lowStats.pass,outcome:selected!==null&&lowStats.pass?"OPEN_AUDIO_TO_MIDI_QA_INTEGRATION":"KEEP_AUDIO_TO_MIDI_UNPROMOTED_AND_REVIEW_METHOD"}};
}

function html(){return `<!doctype html><html lang="it"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>FAME Audio→MIDI Human QA</title><style>
body{margin:0;background:#0b0e12;color:#edf2f7;font-family:system-ui,Segoe UI,Arial,sans-serif}main{max-width:1000px;margin:auto;padding:24px}.panel{background:#141a22;border:1px solid #2d3744;border-radius:14px;padding:18px;margin:14px 0}.muted{color:#9aa8b6}audio{width:100%;margin:5px 0 13px}.score{display:flex;gap:8px}.score button,button{background:#202936;border:1px solid #46566a;color:white;padding:9px 14px;border-radius:8px;cursor:pointer}.score button.sel{background:white;color:#111;font-weight:800}textarea{width:100%;min-height:55px;background:#0e1319;color:white;border:1px solid #354252;border-radius:8px;padding:8px}.row{display:flex;justify-content:space-between;gap:12px;align-items:center}.hidden{display:none}.warn{color:#ffd27a}.progress{height:7px;background:#202733;border-radius:8px}.bar{height:100%;background:#eef2f5;width:0;border-radius:8px}</style></head><body><main>
<h1>FAME — Audio→MIDI Human QA</h1><p class="muted">I due candidati drums sono ciechi. Valuta timing, ruoli e utilità, non il timbro sintetico.</p><div class="progress"><div id="bar" class="bar"></div></div><div id="app"></div><div id="done" class="panel hidden"><h2>Review completa</h2><button id="submit">Salva e calcola gate</button><pre id="result"></pre></div>
<script>
let PKG,idx=0,answers={};const app=document.getElementById("app");
function state(id){return answers[id]||(answers[id]={drums:{A:{score:null,note:""},B:{score:null,note:""}},lowEnd:{score:null,note:""},reviewerAttested:false})}
function buttons(key,value){return [0,1,2,3].map(n=>'<button data-key="'+key+'" data-score="'+n+'" class="'+(value===n?'sel':'')+'">'+n+'</button>').join('')}
function persist(){if(idx>=PKG.families.length)return;const s=state(PKG.families[idx].sourceRecordId);s.drums.A.note=document.getElementById('noteA').value;s.drums.B.note=document.getElementById('noteB').value;s.lowEnd.note=document.getElementById('noteL').value;s.reviewerAttested=document.getElementById('attest').checked}
function render(){if(idx>=PKG.families.length){app.innerHTML='';document.getElementById('done').classList.remove('hidden');document.getElementById('bar').style.width='100%';return}const f=PKG.families[idx],s=state(f.sourceRecordId);document.getElementById('bar').style.width=(idx/PKG.families.length*100)+'%';app.innerHTML='<div class="panel"><div class="row"><h2>'+f.sourceRecordId+'</h2><b>'+(idx+1)+'/8</b></div><h3>Reference drums stem</h3><audio controls src="/media/'+f.sourceRecordId+'/drums"></audio><h3>Drums candidate A</h3><audio controls src="/render/'+f.sourceRecordId+'/A"></audio><div class="score">'+buttons('A',s.drums.A.score)+'</div><textarea id="noteA" placeholder="Nota A opzionale">'+s.drums.A.note+'</textarea><h3>Drums candidate B</h3><audio controls src="/render/'+f.sourceRecordId+'/B"></audio><div class="score">'+buttons('B',s.drums.B.score)+'</div><textarea id="noteB" placeholder="Nota B opzionale">'+s.drums.B.note+'</textarea><p class="muted">Drums: kick retention/falsi kick, snare/hat, timing, utilità Drum View.</p><hr><h3>Reference bass stem</h3><audio controls src="/media/'+f.sourceRecordId+'/bass"></audio><h3>pYIN — MIDI notes render</h3><audio controls src="/render/'+f.sourceRecordId+'/bass-midi"></audio><h3>pYIN — contour diagnostic</h3><audio controls src="/render/'+f.sourceRecordId+'/bass-contour"></audio><div class="score">'+buttons('L',s.lowEnd.score)+'</div><textarea id="noteL" placeholder="Nota low-end opzionale">'+s.lowEnd.note+'</textarea><p class="muted">Low-end: onset, pitch/ottava, durata, glide/contour, utilità MIDI.</p><label><input id="attest" type="checkbox" '+(s.reviewerAttested?'checked':'')+'> Ho ascoltato reference, A, B e low-end e confermo i punteggi.</label><div class="row"><button id="prev" '+(idx===0?'disabled':'')+'>Indietro</button><button id="next">Salva e avanti</button></div><p id="err" class="warn"></p></div>';app.querySelectorAll('[data-key]').forEach(b=>b.onclick=()=>{persist();const k=b.dataset.key,n=Number(b.dataset.score);if(k==='L')s.lowEnd.score=n;else s.drums[k].score=n;render()});document.getElementById('prev').onclick=()=>{persist();idx--;render()};document.getElementById('next').onclick=()=>{persist();if(s.drums.A.score===null||s.drums.B.score===null||s.lowEnd.score===null||!s.reviewerAttested){document.getElementById('err').textContent='Servono i tre punteggi e la conferma di ascolto.';return}idx++;render()}}
document.getElementById('submit').onclick=async()=>{const payload={schema:'fame-owned-beats-audio-to-midi-human-review-submission-v1',version:1,reviewId:PKG.reviewId,packageDigestSha256:PKG.packageDigestSha256,submittedAt:new Date().toISOString(),families:PKG.families.map(f=>({sourceRecordId:f.sourceRecordId,...answers[f.sourceRecordId]}))};const r=await fetch('/submit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});document.getElementById('result').textContent=await r.text();if(r.ok)document.getElementById('submit').disabled=true};(async()=>{PKG=await(await fetch('/package')).json();render()})();
</script></main></body></html>`}

function openBrowser(url){try{let child;if(process.platform==="win32")child=spawn("cmd.exe",["/c","start","",url],{detached:true,stdio:"ignore"});else if(process.platform==="darwin")child=spawn("open",[url],{detached:true,stdio:"ignore"});else child=spawn("xdg-open",[url],{detached:true,stdio:"ignore"});child.unref()}catch{}}
function serve(workspaceRoot,reviewId=DEFAULT_REVIEW_ID,port=0){
 const workspace=path.resolve(workspaceRoot);qa.technical(workspace,RUN_ID);const p=protocol(),root=reviewRoot(workspace,reviewId),packageFile=path.join(root,"review-package.json"),keyFile=path.join(root,"blind-key.json");if(!fs.existsSync(packageFile)||!fs.existsSync(keyFile))throw new Error("Human Review package missing; run prepare first");
 const pkg=readJson(packageFile),key=readJson(keyFile),publicPkg={...pkg,packageDigestSha256:sha256File(packageFile)},submissionFile=path.join(root,"submission.json"),reportFile=path.join(root,"report.json");if(fs.existsSync(submissionFile)||fs.existsSync(reportFile))throw new Error("Human Review already finalized");const cache=new Map();
 const server=http.createServer((req,res)=>{try{const url=new URL(req.url,"http://127.0.0.1");if(req.method==="GET"&&url.pathname==="/"){res.writeHead(200,{"Content-Type":"text/html; charset=utf-8","Cache-Control":"no-store"});return res.end(html())}if(req.method==="GET"&&url.pathname==="/package"){res.writeHead(200,{"Content-Type":"application/json","Cache-Control":"no-store"});return res.end(stableJson(publicPkg))}const parts=url.pathname.split("/").filter(Boolean).map(decodeURIComponent);if(req.method==="GET"&&parts[0]==="media"&&parts.length===3){const file=sourceStemPath(workspace,parts[1],parts[2]);if(!fs.existsSync(file))throw new Error("Stem missing");res.writeHead(200,{"Content-Type":"audio/wav","Cache-Control":"no-store"});return fs.createReadStream(file).pipe(res)}if(req.method==="GET"&&parts[0]==="render"&&parts.length===3){const rid=parts[1],what=parts[2],cacheKey=rid+"|"+what;let wav=cache.get(cacheKey);if(!wav){const result=resultFor(workspace,rid);if(what==="A"||what==="B"){const mapped=key.mapping[rid]?.[what];if(!mapped)throw new Error("Blind mapping missing");wav=renderDrums(result[mapped].events)}else if(what==="bass-midi")wav=renderBassNotes(result.lowEndPyin.notes);else if(what==="bass-contour")wav=renderContour(result.lowEndPyin.pitchContour);else throw new Error("Unknown render target");cache.set(cacheKey,wav)}res.writeHead(200,{"Content-Type":"audio/wav","Cache-Control":"no-store"});return res.end(wav)}if(req.method==="POST"&&url.pathname==="/submit"){let body="";req.setEncoding("utf8");req.on("data",chunk=>{body+=chunk;if(body.length>1024*1024)req.destroy()});return req.on("end",()=>{try{const doc=validateSubmission(JSON.parse(body),publicPkg),finalized=finalizeSubmission(doc,publicPkg,key,p),submission={...doc,unblinded:finalized.families,gate:finalized.gate};fs.writeFileSync(submissionFile,stableJson(submission),{flag:"wx"});const report={schema:REPORT_SCHEMA,version:1,reviewId,runId:RUN_ID,completedAt:new Date().toISOString(),packageDigestSha256:publicPkg.packageDigestSha256,submissionDigestSha256:sha256File(submissionFile),records:8,gate:finalized.gate,safety:{finalHoldoutAccessed:false,batch131Accessed:false,trainingAuthorized:false,taskDataReadyMayBeDeclared:false},nextAction:"PREPARE_BASIC_PITCH_ARM_AND_COMPARE_BEFORE_FINAL_LOW_END_PROMOTION"};fs.writeFileSync(reportFile,stableJson(report),{flag:"wx"});res.writeHead(200,{"Content-Type":"application/json"});res.end(stableJson(report));setTimeout(()=>server.close(),750)}catch(error){res.writeHead(400,{"Content-Type":"text/plain"});res.end(error.message)}})}res.writeHead(404);res.end("Not found")}catch(error){res.writeHead(500,{"Content-Type":"text/plain"});res.end(error.message)}});
 server.on("error",error=>{process.stderr.write("AUDIO TO MIDI HUMAN REVIEW SERVER FAILED: "+(error.code||"ERROR")+": "+error.message+"\n");process.exitCode=1});server.on("listening",()=>{const address=server.address(),boundPort=address&&typeof address==="object"?address.port:Number(port),url="http://127.0.0.1:"+boundPort+"/";process.stdout.write(stableJson({mode:"AUDIO_TO_MIDI_HUMAN_REVIEW_SERVER_READY",reviewId,url,records:8,drumsArmIdentityExposedToReviewer:false,finalHoldoutAccessedByThisCommand:false,trainingAuthorized:false}));openBrowser(url)});server.listen(Number(port),"127.0.0.1");
}
function report(workspaceRoot,reviewId=DEFAULT_REVIEW_ID){const file=path.join(reviewRoot(path.resolve(workspaceRoot),reviewId),"report.json");if(!fs.existsSync(file))throw new Error("Human Review report missing");return readJson(file)}
function main(args=process.argv.slice(2)){const[command,workspace,reviewId,port]=args;if(!command||!workspace)throw new Error("Usage: node audio-to-midi-human-review.js <prepare|serve|report> <workspace> [review-id] [port]");if(command==="prepare")process.stdout.write(stableJson(prepare(workspace,reviewId||DEFAULT_REVIEW_ID)));else if(command==="serve")serve(workspace,reviewId||DEFAULT_REVIEW_ID,port===undefined?0:Number(port));else if(command==="report")process.stdout.write(stableJson(report(workspace,reviewId||DEFAULT_REVIEW_ID)));else throw new Error("Unknown command")}
if(require.main===module){try{main()}catch(error){console.error(error.message);process.exitCode=1}}
module.exports={prepare,median,computeArmStats,chooseDrums,validateSubmission,finalizeSubmission,renderDrums,renderBassNotes,renderContour};
